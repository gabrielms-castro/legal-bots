export type LoginOptions = {
    loginURL?:string,
    userSelector?: string,
    passSelector?: string,
    submitSelector?: string,
    username?: string,
    password?: string,
    otpSelector?: string,
    postLoginSelector?: string,
}